import { ValidateCalendarDirective } from './validate-calendar.directive';

describe('ValidateCalendarDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateCalendarDirective();
    expect(directive).toBeTruthy();
  });
});
